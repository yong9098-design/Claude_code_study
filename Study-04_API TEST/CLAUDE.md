# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This project is in early setup. Currently, only a `.env` file exists with an `OPENROUTER_API_KEY` configured.

## Environment

- **API**: OpenRouter (`OPENROUTER_API_KEY` in `.env`)
- Keep `.env` out of version control — add it to `.gitignore` before initializing a git repo.
